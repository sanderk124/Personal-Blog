<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>New Article</title>
  <style>
    /* Basic Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #f8f8f8;
      padding: 20px;
    }

    /* Container for the form */
    .new-article-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      border: 2px solid #ccc;
      border-radius: 5px;
    }

    .new-article-container h1 {
      font-size: 1.8em;
      margin-bottom: 20px;
    }

    /* Form elements */
    .new-article-form label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }

    .new-article-form input[type="text"],
    .new-article-form input[type="date"],
    .new-article-form textarea {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #aaa;
      border-radius: 4px;
      font-size: 1em;
    }

    .new-article-form textarea {
      min-height: 150px;
      resize: vertical; /* Allows user to resize the content area if needed */
    }

    .new-article-form button {
      display: inline-block;
      padding: 10px 20px;
      font-size: 1em;
      background-color: #007BFF;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .new-article-form button:hover {
      background-color: #0056b3;
    }

  </style>
</head>
<body>

  <div class="new-article-container">
    <h1>Update Article</h1>
    <form class="new-article-form" action="/new" method="POST">
      
      <label for="article-title">Article Title</label>
      <input 
        type="text" 
        id="article-title" 
        name="article_title" 
        placeholder="Enter the article title" 
        required
      />

      <label for="article-author">Article Author</label>
      <input 
        type="text" 
        id="article-title" 
        name="article_Author" 
        placeholder="Enter the article title" 
        required
      />

      <label for="content">Content</label>
      <textarea 
        id="content" 
        name="content" 
        placeholder="Write your article content here..."
        required
      ></textarea>

      <button type="submit">Publish</button>

    </form>
  </div>

</body>
</html>
