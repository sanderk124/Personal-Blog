<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Article - <%= locals.article.title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background: #fff;
            max-width: 700px;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 24px;
            color: #222;
            margin-bottom: 10px;
        }

        h2 {
            font-size: 18px;
            color: #555;
            margin: 5px 0;
        }

        .meta {
            font-size: 14px;
            color: #777;
            margin-bottom: 15px;
        }

        p {
            font-size: 16px;
            line-height: 1.6;
        }

        hr {
            border: none;
            height: 1px;
            background: #ddd;
            margin: 15px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= locals.article.title %></h1>
        <h2>By <span class="meta"><%= locals.article.author %></span></h2>
        <h3 class="meta">Published on <%= locals.article.publishedDate %></h3>
        <hr>
        <p><%= locals.article.content %></p>
    </div>
</body>
</html>
