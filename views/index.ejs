<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Blog</title>
    <style>
        /* Reset and Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            color: #333;
            padding: 20px;
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #333;
            color: white;
            padding: 15px 20px;
            border-radius: 8px;
        }

        .navbar h1 {
            font-size: 24px;
        }

        .navbar a {
            text-decoration: none;
            color: white;
            font-weight: bold;
            background: #007BFF;
            padding: 8px 15px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .navbar a:hover {
            background: #0056b3;
        }

        /* Blog Container */
        .blog-container {
            max-width: 800px;
            margin: 30px auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        .blog-container h2 {
            margin-bottom: 10px;
        }

        .blog-container ul {
            list-style: none;
            padding: 0;
        }

        .blog-container li {
            background: #fff;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 8px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
        }

        .blog-container a {
            display: inline-block;
            margin-right: 10px;
            text-decoration: none;
            color: #007BFF;
            font-weight: bold;
            transition: 0.3s;
        }

        .blog-container a:hover {
            color: #0056b3;
            text-decoration: underline;
        }

        .no-articles {
            text-align: center;
            font-size: 18px;
            color: #777;
            margin-top: 20px;
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .blog-container {
                width: 100%;
                padding: 15px;
            }

            .navbar {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }

            .navbar a {
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <div class="navbar">
        <h1>Personal Blog</h1>
        <% if (able_to_edit == true) { %>
        <a href="/new">+ Add New Post</a>
        <% } %>
    </div>

    <!-- Blog Container -->
    <div class="blog-container">
        <% if (!locals.articles || locals.articles.length === 0) { %>
            <h2 class="no-articles">No articles to show</h2>
        <% } else { %>
            <ul>
                <% locals.articles.map((post) => { %>
                <li>
                    <h2><%= post.title %></h2>
                    <a href="/post/<%= post.id %>">View Post</a>  
                    <% if(able_to_edit == true) { %>
                    <a href="/edit/<%= post.id %>">Edit</a>
                    <% } %>
                </li>
                <% }) %>
            </ul>
        <% } %>
    </div>

</body>
</html>
